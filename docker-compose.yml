version: "3.9"

services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"

  server:
    build: ./server
    ports:
      - "3001:3001"
    depends_on:
      - redis

  client:
    build: ./client
    ports:
      - "3000:3000"
    depends_on:
      - server
